# [135] Cover each direction of overlap cleanup

**Date**: 2026-05-05
**Category**: test-mistake

### What went wrong
The first #135 test set covered final-to-final overlap and the full issue-shaped transcript, but did not force the suffix-only interim cleanup branch. The coverage gate caught the untested helper path after implementation.

### Correct approach
For bidirectional transcript overlap cleanup, add focused regressions for prefix-only trimming, suffix-only trimming, and the surrounding-final sandwich case before relying on the full meeting-shaped regression.

### How to avoid
When adding text-normalization helpers, map each helper branch to at least one minimal test fixture that can only pass by exercising that branch.
